// Function Currying

// Currying is a process in functional programming in
// which we transform a function with multiple arguments
// into a sequence of nesting functions that takes one
// argument at a time.
// function f(a, b, c) is transformed to f(a)(b)(c)
